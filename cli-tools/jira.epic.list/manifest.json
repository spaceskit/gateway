{
  "schemaVersion": 1,
  "id": "jira.epic.list",
  "displayName": "Jira Epic List",
  "description": "List Jira epics, or list issues inside one epic, with compact summary rows by default and raw Jira JSON optionally.",
  "bundleId": "jira-cli",
  "bundleDisplayName": "Jira CLI",
  "bundleDescription": "Gateway-managed Jira CLI bundle for account discovery plus project, board, release, epic, sprint, and issue operations.",
  "toolGroupId": "epics",
  "toolGroupDisplayName": "Epics",
  "executable": "/Users/caruso/code/spaces/gateway/scripts/jira-cli-tools/spaces-jira.mjs",
  "resolvedExecutable": "/Users/caruso/code/spaces/gateway/scripts/jira-cli-tools/spaces-jira.mjs",
  "argsTemplate": [
    "--op",
    "epic.list",
    "--payload",
    "{{payload}}"
  ],
  "inputSchema": {
    "type": "object",
    "properties": {
      "payload": {
        "type": "object",
        "properties": {
          "project": {
            "type": "string",
            "description": "Optional Jira project key or id override for this call."
          },
          "query": {
            "type": "string",
            "description": "Optional free-text search query."
          },
          "jql": {
            "type": "string",
            "description": "Optional raw JQL query."
          },
          "type": {
            "type": "string",
            "description": "Optional Jira issue type filter."
          },
          "status": {
            "type": "array",
            "description": "Optional issue status filters.",
            "items": {
              "type": "string"
            }
          },
          "priority": {
            "type": "string",
            "description": "Optional issue priority filter."
          },
          "reporter": {
            "type": "string",
            "description": "Optional reporter email or display name."
          },
          "assignee": {
            "type": "string",
            "description": "Optional assignee email or display name."
          },
          "component": {
            "type": "string",
            "description": "Optional component filter."
          },
          "labels": {
            "type": "array",
            "description": "Optional issue labels.",
            "items": {
              "type": "string"
            }
          },
          "parent": {
            "type": "string",
            "description": "Optional parent issue key."
          },
          "history": {
            "type": "boolean",
            "description": "Whether to limit results to recently accessed issues."
          },
          "watching": {
            "type": "boolean",
            "description": "Whether to limit results to watched issues."
          },
          "created": {
            "type": "string",
            "description": "Optional created date filter, for example `today`, `-7d`, or `2026-03-01`."
          },
          "updated": {
            "type": "string",
            "description": "Optional updated date filter."
          },
          "createdAfter": {
            "type": "string",
            "description": "Optional lower bound for created date."
          },
          "updatedAfter": {
            "type": "string",
            "description": "Optional lower bound for updated date."
          },
          "createdBefore": {
            "type": "string",
            "description": "Optional upper bound for created date."
          },
          "updatedBefore": {
            "type": "string",
            "description": "Optional upper bound for updated date."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results to return. Defaults to 20 and must be between 1 and 100.",
            "minimum": 1,
            "maximum": 100
          },
          "detail": {
            "type": "string",
            "description": "Result detail level. `compact` returns summary rows only and is the default; `raw` returns upstream Jira JSON for narrow queries.",
            "enum": [
              "compact",
              "raw"
            ]
          },
          "orderBy": {
            "type": "string",
            "description": "Optional field used to order results."
          },
          "reverse": {
            "type": "boolean",
            "description": "Whether to reverse the default order."
          },
          "paginate": {
            "type": "string",
            "description": "Legacy zero-offset pagination string such as `0:50`. Offsets other than zero are rejected because `jira-cli` 1.7.x returns duplicate pages."
          },
          "comments": {
            "type": "integer",
            "description": "Optional number of recent comments to include for `detail: \"raw\"` reads.",
            "minimum": 0
          },
          "epicKey": {
            "type": "string",
            "description": "Optional Jira epic key."
          }
        },
        "additionalProperties": false
      }
    },
    "additionalProperties": false
  },
  "instructions": "Use this tool to inspect epics or the issues inside a specific epic. Keep the default compact mode for scanning and switch to `detail: \"raw\"` only for narrow reads.",
  "examples": [
    {
      "name": "List active epics",
      "description": "Searches epics with status filters.",
      "arguments": {
        "payload": {
          "project": "OPS",
          "status": [
            "In Progress"
          ],
          "limit": 20
        }
      },
      "expectedOutput": "{\n  \"ok\": true,\n  \"operation\": \"epic.list\",\n  \"summary\": \"Listed Jira epics.\"\n}"
    },
    {
      "name": "List issues in an epic",
      "description": "Lists issues for a specific epic.",
      "arguments": {
        "payload": {
          "project": "OPS",
          "epicKey": "OPS-100",
          "limit": 20
        }
      },
      "expectedOutput": "{\n  \"ok\": true,\n  \"operation\": \"epic.list\",\n  \"summary\": \"Listed issues for Jira epic OPS-100.\"\n}"
    }
  ],
  "timeoutMs": 45000,
  "maxOutputBytes": 262144,
  "cwdMode": "fixed",
  "fixedCwd": "/Users/caruso/code/spaces/gateway/scripts/jira-cli-tools",
  "outputMode": "json",
  "dangerLevel": "standard",
  "enabled": true,
  "createdAt": "2026-04-08T20:49:28.676Z",
  "updatedAt": "2026-04-08T20:49:28.676Z"
}
