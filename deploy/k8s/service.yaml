apiVersion: v1
kind: Service
metadata:
  name: spaceskit-gateway
  labels:
    app: spaceskit-gateway
spec:
  type: ClusterIP
  ports:
  - port: 9320
    targetPort: 9320
    protocol: TCP
    name: http
  selector:
    app: spaceskit-gateway
